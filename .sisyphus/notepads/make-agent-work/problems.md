# Problems

## 2026-04-26
- Unresolved: full `go test ./...` cannot complete until module dependency `github.com/bmatcuk/doublestar/v2` is added to `go.mod`/`go.sum`.
- Unresolved: workspace Go environment intermittently fails stdlib resolution for `syscall` on Windows, preventing mandatory full-suite verification (`go test ./...`).
