- 2026-05-07: Web UI mounting can stay isolated from API logic by adding a dedicated `internal/server/web.go` registration path and composing it after the existing `/api/v1` route block in `cmd/server`.
- 2026-05-07: Embedded test fixtures for web mounting are easiest to verify with `//go:embed testdata/web/index.html` plus a `RegisterWebRoutesWithFS` seam so route behavior can be tested without touching production API code.
- 2026-05-07: Session dashboard/history listing can reuse persisted `config_json` task metadata from `sessions` and map domain statuses to stable API buckets (`created/running/completed/cancelled/failed`) so UI list rendering stays decoupled from runtime-only state details.
- 2026-05-07: The embedded shell can stay framework-free by exposing auth and API helpers on window globals, which lets the HTML keep plain script tags while still separating token storage, fetch wrappers, and hash-route orchestration.
- 2026-05-07: Browser-side SSE helpers must use same-origin etch() with the Bearer header instead of EventSource because the existing authenticated /api/v1/sessions/{id}/stream route only accepts JWT via the Authorization header.
- 2026-05-07: Browser JWT bootstrap should re-verify any stored token before unlocking the UI, while connect/disconnect handlers keep the token input intact on failed verification and only clear it on explicit disconnect or auth-required expiry.
- 2026-05-07: The stream view should treat transport loss as a degraded UI state, not a session failure; keep the session ID visible, preserve the detail route, and surface disconnect messaging in a dedicated banner.
- 2026-05-07: Browser SSE token deltas are easiest to read when accumulated into one escaped text buffer, while lifecycle/error/complete events remain separate DOM rows and the container trims to the newest 100 items.
- 2026-05-07: Dashboard history rendering in the plain JS shell is simplest when page/status state is kept in a tiny in-memory object and filter/pagination buttons are rebound after each `innerHTML` refresh.
- 2026-05-07: Inspect/detail views can stay framework-free by normalizing status colors, timestamps, plan summaries, and provenance serialization through shared helper functions before injecting escaped HTML cards.
- 2026-05-07 (T10): Documentation sync for v5 follows the established v2/v3/v4 pattern: README gets header blurb update + feature section + quick-start flag + "当前仍不包含" update; PROGRESS gets T1-T10 task table + non-goals; USAGE gets browser flow walkthrough + session list API curl examples; validation-matrix gets feature rows + non-goal verification + acceptance commands. All four files must explicitly disclaim WebSocket, replay, separate SPA, and login — these are the most common misattributions.
