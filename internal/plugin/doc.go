// Package plugin defines the tool-plugin contract used by the runtime.
//
// v2 intentionally limits plugins to tool capabilities only. Provider,
// agent, verifier, marketplace, and loading/discovery concerns are handled in
// later tasks.
package plugin
